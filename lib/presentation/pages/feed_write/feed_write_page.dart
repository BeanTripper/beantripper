import 'package:flutter/material.dart';

class FeedWritePage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('FeedWritePage'),
      ),
    );
  }
}
